name: Bug report / 错误报告
about: Use this template to report bugs. Please provide reproduction steps and logs. / 使用此模板报告程序错误，请提供复现步骤与日志。
title: '[BUG] '
labels: bug
assignees: ''

<!--
双语 Issue 模板 — 中文 / English
请在提交时尽量完整填写环境与复现步骤，便于快速定位问题。
-->

# 中文（Chinese）

## 环境
- Windows 版本（例如 Windows 10 21H2）：
- Java 版本（运行 `java -version` 并粘贴输出）：
- Minecraft Server 版本（例如 1.20.1）：
- MCServerManager 版本（Release 名称或 commit id）：

## 复现步骤
请按步骤列出可复现问题的最少操作：
1. 
2. 
3. 

预期行为：  
实际行为：  

## 日志 / 输出 / 截图
请附上控制台输出、plugin.log、start.bat 输出或截图（必要时请压缩为 zip 上传）。如有大段文本请使用代码块粘贴。

示例：
```
（在此粘贴错误堆栈 / 日志）
```

## 是否使用插件（Plugins）
- 是 / 否  
若是，请粘贴对应插件的 plugin/config.json（如包含敏感信息请先删除）并说明插件名称与版本。

## 其他信息
- 是否可在一个干净目录（仅放 MCServerManager.exe）下复现？（是/否）
- 已尝试的临时修复（例如：升级/降级 Java、更换版本名）：
- 联系方式（可选）：

---

# English

## Environment
- Windows version (e.g. Windows 10 21H2):
- Java version (please run `java -version` and paste output):
- Minecraft Server version (e.g. 1.20.1):
- MCServerManager version (release name or commit id):

## Reproduction steps
List the minimal steps to reproduce the bug:
1.
2.
3.

Expected behavior:  
Actual behavior:  

## Logs / Output / Screenshots
Please attach console output, plugin.log, start.bat output or screenshots. Zip large files. For long text, paste inside code blocks.

Example:
```
(paste error stack trace / logs here)
```

## Plugins
- Using plugins? Yes / No  
If yes, please paste the plugin's plugin/config.json (remove sensitive info) and state plugin name and version.

## Additional information
- Can this be reproduced in a clean folder (only MCServerManager.exe present)? Yes / No  
- Workarounds tried (e.g. Java upgrade/downgrade, change version name):
- Contact info (optional):
